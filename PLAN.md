# My Pretty Star — Plan

## Problem
You have an old computer you want to use as a personal server (file storage, local AI inference), but your ISP doesn't provide a public IP. Renting a VPS/VDS with similar specs is too expensive.

## Solution
Rent a cheap VPS/VDS to get a public IP. Set up a site-to-site VPN (star topology) with the VPS as the hub. Your server and workstation connect to the hub, creating an improvised local network. You can then access your server via a "local" VPN IP.

## Architecture

```
VPS (Hub)          Old PC (Server)      Laptop (Client)
10.0.0.1 ───────── 10.0.0.2             10.0.0.3
    │                     │                    │
    ├─ AmneziaWG ─────────┼────────────────────┘
    │   Docker Compose    │ Docker Compose      Native WG GUI
    │                     │                     or mps CLI
```

## Technology Stack

| Component | Choice |
|-----------|--------|
| Language | **Go** (single binary, cross-compilation) |
| VPN | **AmneziaWG** (obfuscation + WG-compatible conf) |
| Deployment | **Docker Compose** for hub and server; client uses CLI or native WG client |
| CLI | Single `mps` command |
| Modules | Interface-based; core module in MVP |
| Security | **Master password** (AES-GCM) protecting node configs |

## CLI Commands

| Command | Description |
|---------|-------------|
| `mps hub init [--docker] [--pass]` | Init VPS as hub (generate configs, keys, docker-compose) |
| `mps hub status` | List connected peers |
| `mps node join <hub> [--docker]` | Connect a node (auto via CLI) |
| `mps node status` | VPN connection status |
| `mps module list` | List available/enabled modules |
| `mps module enable <name>` | Enable a module |
| `mps module disable <name>` | Disable a module |
| `mps decrypt <file>` | Decrypt an encrypted config |
| `mps doctor` | System diagnostics |
| `mps version` | Print version |

## Module System

```go
type Module interface {
    Name() string
    Priority() int
    Dependencies() []string
    Init(*Context) error
    Start() error
    Stop() error
    Status() Status
}
```

MVP: only `core` module (VPN + routing + SSH).

## Security

- WireGuard uses Curve25519 keys (not passwords) — cryptographically secure
- Node configs are encrypted with **master password** (AES-256-GCM)
- Hub config is not encrypted (runs on VPS)
- User can either:
  1. Run `mps node join` (prompts for master password)
  2. Copy `.enc` file and decrypt manually with `mps decrypt`

## Docker

Two images:
- `mps/hub` — hub with AmneziaWG + peer management
- `mps/node` — node (AmneziaWG client)
- Both require `NET_ADMIN`, `SYS_MODULE` capabilities
- docker-compose.yml generated by `mps hub init --docker` and `mps node join --docker`

## User Flow

1. Install `mps` (curl → script)
2. On VPS: `mps hub init --docker && docker compose up -d`
3. On server: `mps node join --hub 1.2.3.4 --docker && docker compose up -d`
4. On laptop: `mps node join --hub 1.2.3.4` **or** import `.conf` in WG GUI
5. Result: `ping 10.0.0.2 && ssh user@10.0.0.2`

## MVP Delivery Plan

| # | Task | Description |
|---|------|-------------|
| 1 | Go project structure | `cmd/`, `pkg/`, `modules/`, go.mod, deps |
| 2 | CLI framework | cobra commands: hub, node, module, doctor |
| 3 | Module system | Module interface, lifecycle, context |
| 4 | AmneziaWG core | Key generation, config templates, interface management |
| 5 | Master password | AES-256-GCM encrypt/decrypt configs |
| 6 | Docker images | Dockerfile for hub and node |
| 7 | Docker-compose gen | `--docker` flag generates compose.yml |
| 8 | Setup scripts | quick-start.sh, bootstrap.sh |
| 9 | Documentation | README with step-by-step guide |
| 10 | Unit tests | Core logic tests |
